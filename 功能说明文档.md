# EIOT 物联网综合管理平台 — 功能说明文档

> 版本：v2.0　状态：完整实现飞燕平台/云智能App核心功能　更新日期：2026-08-30

---

## 一、项目定位

EIOT（Easy IoT）是一款**轻量化、高性能、响应式**的物联网设备综合管理平台。面向中小规模物联网场景，提供从**设备接入 → 数据采集 → 状态监控 → 控制下发 → 设备共享 → 移动端管理**的完整闭环。

核心设计理念：

| 理念 | 含义 |
|------|------|
| 产品即物模型 | 一个产品 = 一套独立物模型 + 一套移动端 UI 模板，三者合一，结构极简 |
| 严格多租户 | 用户数据完全隔离，仅可通过设备共享机制跨用户授权 |
| 云原生部署 | Docker Compose 一键拉起，Alpine 基础镜像，秒级启动 |
| 全响应式 | Web 管理后台适配桌面 / 平板 / 手机三端 |
| 可视化拖拽 | 产品级移动端 UI 通过拖拽方式生成，无需编写前端代码 |

平台整体架构示意：

```
┌────────────────────────────────────────────────────────────┐
│                    移动端 App (Android)                      │
│       Kotlin + WebView + 原生 HTTP，/android 目录            │
└────────────────────────────────┬───────────────────────────┘
                                 │ HTTPS
┌────────────────────────────────▼───────────────────────────┐
│                   前端管理台 (Web)                           │
│   Vue3 + Vite + Element Plus + ECharts，/frontend 目录      │
└────────────────────────────────┬───────────────────────────┘
                                 │ RESTful API（端口 8080）
┌────────────────────────────────▼───────────────────────────┐
│                   后端服务 (Go + Gin)                        │
│  Gin 路由 / GORM / JWT，/backend 目录                         │
└──────────────┬──────────────────┬──────────────────┬────────┘
               │                  │                  │
        ┌──────▼─────┐     ┌──────▼─────┐    ┌──────▼─────┐
        │   MySQL     │     │   Redis    │    │   EMQX     │
        │ 业务数据    │     │  缓存/验证  │    │  MQTT 消息  │
        └─────────────┘     └─────────────┘    └─────────────┘
```

---

## 二、技术栈详解

### 2.1 后端（Go + Gin）

| 技术领域 | 选型 | 版本 | 说明 |
|----------|------|------|------|
| 编程语言 | Go | 1.22 | 静态编译、GC、goroutine 原生并发 |
| Web 框架 | Gin | 1.10 | 高性能 HTTP 路由，`:id` 参数语法 |
| ORM | GORM | latest | AutoMigrate 自动建表 + 事务 |
| 关系数据库 | MySQL | 8.0 | 用户/产品/设备等结构化数据 |
| 缓存 | Redis | 7.2 | 设备在线状态、短信验证码、限流 |
| MQTT Broker | EMQX | 5.7 | 设备消息接入/下发，Dashboard 18083 |
| 鉴权 | JWT | - | HS256 签名，`Authorization: Bearer xxx` |
| 配置管理 | YAML | - | `config.yaml` 集中管理环境配置 |
| 密码安全 | bcrypt | - | 单向哈希，成本因子 14 |
| 交叉编译 | GOOS/GOARCH | linux/amd64 | Windows 开发机 → Linux 容器 |
| 容器 | Alpine | 3.19 | 基础镜像体积小，安全补丁齐全 |

代码模块组织：

```
backend/
├── cmd/api-server/main.go            # 入口：初始化依赖、启动 Gin
├── internal/
│   ├── dao/mysql.go                  # 数据库连接与单例
│   ├── handler/handlers.go           # 所有路由注册 + 请求处理函数
│   ├── logic/business.go             # 业务实现（创建产品/鉴权/共享等）
│   ├── logic/seed.go                 # 首次启动插入演示数据
│   ├── model/models.go               # GORM 数据模型（User/Product/Device 等）
│   └── svc/servicecontext.go         # 服务上下文（配置/数据库/缓存等）
└── pkg/
    ├── config/config.go              # YAML 配置解析
    ├── middleware/auth.go            # JWT 鉴权 + 管理员中间件
    ├── cache/redis.go                # Redis 封装
    ├── mqtt/emqx.go                  # EMQX 消息发布接口
    ├── util/crypto.go                # bcrypt + AES-CBC
    └── tsdb/tdengine.go              # 时序数据存储（预留 TDengine 接口）
```

### 2.2 前端（Vue3 + Vite）

| 技术领域 | 选型 | 版本 | 说明 |
|----------|------|------|------|
| 前端框架 | Vue | 3.4 | `<script setup>` 组合式 API |
| 构建工具 | Vite | 5.x | 冷启动 < 300ms，HMR 毫秒级 |
| UI 组件库 | Element Plus | latest | 表格/表单/抽屉/对话框/图表容器 |
| 图表引擎 | ECharts | 5.x | 温湿度/能耗等趋势曲线 |
| 路由 | Vue Router | 4.x | 管理台 SPA 页面导航 |
| HTTP 客户端 | Axios | latest | 请求拦截自动注入 Token |
| 部署方式 | Nginx | 1.25-alpine | 静态资源服务 + gzip |

关键页面文件：

| 页面 | 文件 | 功能 |
|------|------|------|
| 登录页 | [Login.vue](file:///d:/AI/eIOT/frontend/src/views/Login.vue) | 管理员/用户登录入口 |
| 全局布局 | [Layout.vue](file:///d:/AI/eIOT/frontend/src/views/Layout.vue) | 响应式侧边栏 + 顶栏 + 抽屉菜单 |
| 仪表盘 | [Dashboard.vue](file:///d:/AI/eIOT/frontend/src/views/Dashboard.vue) | 产品数/设备数/在线数/用户数统计 |
| 项目管理 | [Project.vue](file:///d:/AI/eIOT/frontend/src/views/Project.vue) | 项目 CRUD（飞燕三级层级） |
| 产品管理 | [Product.vue](file:///d:/AI/eIOT/frontend/src/views/Product.vue) | 产品 + 物模型一体化管理 |
| 设备管理 | [Device.vue](file:///d:/AI/eIOT/frontend/src/views/Device.vue) | 响应式表格/卡片切换 |
| 设备详情 | [DeviceDetail.vue](file:///d:/AI/eIOT/frontend/src/views/DeviceDetail.vue) | 最新数据 + ECharts 趋势 + 控制面板 |
| 移动端 UI 配置 | [MobileUI.vue](file:///d:/AI/eIOT/frontend/src/views/MobileUI.vue) | 拖拽式 UI 组件编排 |
| 设备共享 | [Share.vue](file:///d:/AI/eIOT/frontend/src/views/Share.vue) | 管理共享关系 |
| 用户管理 | [Users.vue](file:///d:/AI/eIOT/frontend/src/views/Users.vue) | 普通用户列表 |

### 2.3 移动端（Android）

| 项 | 选型 |
|----|------|
| 平台 | Android（雷电模拟器 / LDPlayer 9） |
| 开发语言 | Kotlin |
| UI 框架 | 原生 XML Layout + WebView 混合 |
| 网络 | OkHttp + Retrofit 风格的 HTTP 客户端 |
| 后端地址 | `http://192.168.31.29:8080` |
| 目录 | `/android` |

功能模块：
1. **原生登录页**：手机号 + 密码 / 短信验证码登录
2. **设备列表页**：当前用户所有设备，展示在线状态、设备名、产品类型
3. **设备详情页**：根据产品物模型动态渲染控件，支持数据展示、开关/滑杆控制
4. **设备共享页**：输入目标手机号进行授权，或撤销已共享
5. **管理员模式**（登录为 admin 时自动启用）：产品管理 + 物模型配置 + 移动端 UI 拖拽配置

---

## 三、核心功能模块

### 3.1 用户认证与权限

**用户角色**：

| 角色 | 权限 |
|------|------|
| `admin` | 产品（物模型）管理、批量设备管理、用户管理、看板统计、移动端 UI 配置、规则引擎、设备分组/标签、网关子设备、配网、数据流转、协议网关、设备诊断 |
| `user` | 登录/注册、绑定设备、使用设备、共享设备、场景执行、家庭/房间管理 |

**认证流程**：

1. 客户端提交 `POST /api/auth/login`，携带 `{ phone, password }`
2. 后端校验 bcrypt 哈希，成功后签发 JWT（内含 `uid`、`role`、过期时间 7 天）
3. 后续请求在 `Authorization: Bearer <token>` 中携带
4. `middleware.AuthMiddleware` 解析并注入当前用户上下文
5. 管理员接口额外通过 `middleware.AdminMiddleware` 校验 `role == "admin"`

**新增认证接口**：

| 方法 | 路由 | 说明 |
|------|------|------|
| POST | `/api/auth/register` | 手机号+密码注册 |
| POST | `/api/auth/refresh` | 刷新 Access Token |

**演示账号**（首次启动由 seed 逻辑自动创建）：

| 账号 | 密码 | 角色 |
|------|------|------|
| `13800000000` | `admin123` | 管理员 |
| `13800000001` | `user123` | 普通用户 |
| `13800000002` | `user123` | 普通用户 |
| `13800000003` | `user123` | 普通用户 |
| `13800000004` | `user123` | 普通用户 |
| `13800000005` | `user123` | 普通用户 |

---

### 3.2 项目管理（飞燕标准层级）

**三级结构**：项目（Project）→ 产品（Product）→ 设备（Device）

项目用于**按业务场景分组**，一个项目下可放置多款同类智能产品。例如「新手智能家居测试项目」下可包含「智能开关」「温湿度传感器」等多个产品。

| 字段 | 说明 |
|------|------|
| `name` | 项目名称（唯一） |
| `industry` | 行业分类：智能家居 / 工业控制 / 农业大棚 / 智慧城市等 |
| `type` | `consumer`（消费级）/ `commercial`（商用） |
| `description` | 项目描述 |

项目支持 CRUD，删除前需确保项目下无产品。Product 列表支持按 `project_id` 筛选，前端自动加载项目下拉列表。

> 相关文件：[Project.vue](file:///d:/AI/eIOT/frontend/src/views/Project.vue) · [models.go#L5-L17](file:///d:/AI/eIOT/backend/internal/model/models.go#L5-L17)

---

### 3.3 产品管理（产品即物模型一体化）

这是平台的核心抽象——**产品本身就是物模型**。每个产品包含：

| 组成 | 存储字段 | 说明 |
|------|---------|------|
| 基本信息 | `name` / `product_key` / `description` / `icon` | 产品元数据 |
| 项目归属 | `project_id` | 所属项目 ID，0 表示未分类 |
| 联网方式 | `network_type` | `wifi` / `ble` / `cellular` / `wifi_ble` |
| 品类 | `category` | 品类分类（灯具/开关/传感器等） |
| 属性列表 | `properties_json` | 可读可写的物理量（温度、湿度、开关等） |
| 事件列表 | `events_json` | 设备主动上报的事件（故障、告警等） |
| 服务列表 | `services_json` | 可下发的服务命令（重启、校准等） |
| 移动端 UI | `mobile_ui_json` | 管理员拖拽配置的移动端 UI 模板 |

**物模型属性结构**（兼容阿里云飞燕 JSON 格式）：

```json
{
  "identifier": "temp_01",
  "name": "温度",
  "accessMode": "r",
  "dataType": {
    "type": "float",
    "specs": { "min": -40, "max": 85, "step": 0.1, "unit": "℃" }
  }
}
```

支持的 dataType：`bool`（开关）、`int`（整数）、`float`（浮点）、`enum`（枚举）、`text`（文本）。

**产品下有设备时的保护规则**：
- 允许修改 `name` / `description` / `icon` / `mobile_ui_json` / `project_id` / `network_type`
- 禁止修改 `properties_json`（防止历史数据字段不匹配）
- 若需扩展物模型，需先删除该产品下的全部设备

> 相关文件：[Product.vue](file:///d:/AI/eIOT/frontend/src/views/Product.vue)

---

### 3.4 设备管理

**核心字段**：

| 字段 | 说明 |
|------|------|
| `device_sn` | 全局唯一的设备序列号（如 `PK_TEMP001_SN00001`） |
| `device_name` | 设备名称（展示用） |
| `device_secret` | 一机一密密钥（隐藏传输） |
| `product_secret` | 一型一密密钥（同产品共用） |
| `bind_mode` | `device_secret`（一机一密）/ `product_secret`（一型一密） |
| `product_id` | 所属产品 ID |
| `owner_id` | 当前绑定的用户 ID（=0 表示空闲，可被绑定） |
| `status` | 0 离线 / 1 在线 |
| `firmware_ver` | 固件版本（设备上报） |
| `ip_address` | 最近一次上报 IP 地址 |
| `latest_json` | 最新上报的属性快照（JSON） |
| `last_online` | 最近一次在线时间 |

**设备生命周期**：

```
管理员批量生成 → 设备空闲 → 用户扫码/绑定 → 已归属
                             ↑
                   用户手动解除 / 管理员强制解绑
```

**批量生成规则**：
- `POST /api/admin/device/batch`，参数 `{ product_id, count, bind_mode?, auto_bind_user_id? }`
- `bind_mode` 支持 `device_secret`（一机一密，每台独立密钥）或 `product_secret`（一型一密，同产品共用密钥）
- 序列号由 `PK_<产品key>_SN<序号>` 自动生成
- 同一产品下 `device_sn` 唯一，冲突时自动回滚
- 若传 `auto_bind_user_id`，生成后直接绑定到该用户

**设备绑定三元组**（飞燕标准）：
- ProductKey / DeviceName / DeviceSecret（或 ProductSecret）
- 管理员可在产品页点击「绑定码」查看设备三元组及 `eiot://bind?...` 格式的绑定 URL

---

### 3.5 设备控制与数据上报（飞燕标准 MQTT Topic）

**飞燕标准 Topic 体系**：

| 方向 | Topic 格式 | 说明 |
|------|-----------|------|
| 设备上报属性 | `/sys/{ProductKey}/{DeviceName}/prop/post` | 设备主动推送属性数据 |
| 云端下发属性 | `/sys/{ProductKey}/{DeviceName}/prop/set` | 云端控制设备 |
| 影子同步 | `/ext/shadow/{ProductKey}/{DeviceName}/update` | 设备影子 |
| 固件版本上报 | `/ota/device/inform/{ProductKey}/{DeviceName}` | 设备上报固件版本 |

**Alink JSON 格式**（兼容飞燕）：

```json
// 设备上报（prop/post）
{
  "id": "1001",
  "version": "1.0",
  "params": { "temp_01": 25.6, "hum_01": 54 },
  "method": "thing.event.property.post"
}

// 云端下发（prop/set）
{
  "id": "1002",
  "version": "1.0",
  "params": { "power_switch": true },
  "method": "thing.service.property.set"
}
```

**控制指令下发**：

```
POST /api/device/:id/control
Body: { "power_switch": true, "target_temp": 26 }
```

后端处理逻辑：
1. 校验设备归属（必须是 owner 或已被共享且有控制权限）
2. 校验 payload 字段与产品物模型属性定义是否匹配
3. 通过 EMQX 发布到飞燕标准 Topic
4. 写入操作日志
5. 返回 `ok`

**设备数据上报**（设备主动上报给 EMQX）：
- 后端订阅 `/sys/+/+/prop/post`，自动解析 `params` 并更新 Redis 中的 `latest_json`
- 固件版本上报订阅 `/ota/device/inform/+/+`，自动更新 `devices.firmware_ver`

---

### 3.6 设备共享

| 接口 | 说明 |
|------|------|
| `POST /api/device/share` | 创建共享：`{ device_id, to_phone }` |
| `DELETE /api/device/share/:id` | 撤销共享（仅设备 owner 可撤销） |
| `GET /api/device/share` | 查看我的共享（共享给他人 / 他人共享给我） |

规则：
- 仅设备 owner 可发起共享
- 可共享给任意已注册的用户
- 同一设备对同一用户不可重复共享
- 撤销后对方即刻失去设备访问权限

---

### 3.7 移动端 UI 配置

在 [MobileUI.vue](file:///d:/AI/eIOT/frontend/src/views/MobileUI.vue) 中，管理员可：

1. 左侧面板展示该产品物模型中的**全部属性**
2. 拖拽属性到中间预览区形成卡片组件
3. 支持调整组件上下顺序、删除、编辑标题
4. 保存后以 JSON 持久化到 `product.mobile_ui_json`
5. 移动端 App 加载设备时，根据产品 ID 拉取该模板自动渲染 UI

组件类型与属性映射：

| 属性 dataType | 默认移动端组件 | 交互 |
|--------------|--------------|------|
| `bool` | 开关卡片 | 点击切换 |
| `int` / `float` | 数值卡片 + 滑杆 | 拖拽设置值 |
| `enum` | 选择卡片 | 弹出选项 |
| `text` | 文本卡片 | 只读展示 |
| 事件 | 事件时间轴 | 只读，列表方式展示最近事件 |

---

### 3.7 看板与统计

`GET /api/admin/stats` 返回聚合数据，前端 Dashboard 页用 ECharts 渲染：

| 指标 | 计算方式 |
|------|---------|
| 产品总数 | `COUNT(products)` |
| 设备总数 | `COUNT(devices)` |
| 在线设备 | `COUNT(devices WHERE status=1)` |
| 用户总数 | `COUNT(users WHERE role='user')` |
| 各产品设备分布 | `GROUP BY product_id` |
| 近 7 天新增设备 | `WHERE created_at >= NOW() - 7d` |

---

### 3.8 操作日志与审计

| 字段 | 内容 |
|------|------|
| `user_id` | 操作用户 |
| `action` | 动作枚举（LOGIN / CREATE_PRODUCT / DELETE_DEVICE / BIND_DEVICE / SHARE / CONTROL 等） |
| `target` | 目标 ID |
| `detail` | 详细描述 |
| `ip` | 客户端 IP |
| `created_at` | 操作时间 |

用途：审计管理员关键操作、追踪设备控制指令、排查问题。

---

### 3.9 规则引擎/场景联动

核心自动化能力，支持**可视化规则配置**，无需编写代码即可实现设备联动。

**核心模型**：

| 模型 | 说明 |
|------|------|
| `Rule` | 规则定义：名称、类型、触发条件、执行动作、启用状态 |
| `RuleExecution` | 执行记录：触发时间、成功/失败、错误信息、详情 JSON |

**触发条件**：

| 类型 | 说明 | 配置参数 |
|------|------|----------|
| `device_property` | 设备属性触发 | device_sn/product_id、property、operator (>、<、==、>=、<=、!=、changed)、threshold |
| `device_event` | 设备事件触发 | device_sn、event_id |
| `timer` | 定时触发 | cron 表达式（外部调度器处理） |
| `manual` | 手动触发 | 通过 API 手动执行 |

**执行动作**：

| 类型 | 说明 | 配置参数 |
|------|------|----------|
| `device_control` | 设备属性控制 | device_sn、property、value |
| `device_service` | 调用设备服务 | device_sn、service_id、input |
| `scene_run` | 执行场景 | scene_id |
| `notify` | 站内消息通知 | title、content、notify_type (message/push/sms) |
| `webhook` | HTTP 回调 | webhook_url |
| `delay` | 延迟执行 | delay_sec（记录用，实际由前端/调度器处理） |

**规则执行流程**：
1. 设备上报数据 → `EvaluateRule` 评估所有启用规则
2. 匹配触发条件 → 顺序执行动作列表
3. 记录执行结果 → 更新规则统计
4. 支持 WebSocket 实时推送执行状态

**API 接口**：

| 方法 | 路由 | 说明 |
|------|------|------|
| POST | `/api/admin/rule` | 创建规则 |
| GET | `/api/admin/rule` | 规则列表 |
| GET | `/api/admin/rule/:id` | 规则详情 |
| PUT | `/api/admin/rule/:id` | 更新规则 |
| DELETE | `/api/admin/rule/:id` | 删除规则 |
| PUT | `/api/admin/rule/:id/toggle` | 启用/禁用 |
| GET | `/api/admin/rule/:id/execution` | 执行记录 |

---

### 3.10 设备分组/标签/高级搜索

基础管理能力，支持跨产品、跨维度的设备组织与检索。

**设备分组** (`DeviceGroup` / `DeviceGroupMember`)：
- 支持产品级分组 (`product_id > 0`) 与跨产品分组 (`product_id = 0`)
- 分组内设备动态增删
- 批量操作基础（配合规则引擎/批量控制）

**设备标签** (`DeviceTag`)：
- Key-Value 形式，支持任意业务标签（如 `location:warehouse_a`、`owner:dept_01`）
- 设备级标签，一个设备可打多个标签

**高级搜索**：
- 按标签精准检索设备
- 组合查询：分组 + 标签 + 产品 + 在线状态

**API 接口**：

| 方法 | 路由 | 说明 |
|------|------|------|
| POST | `/api/admin/device-group` | 创建分组 |
| GET | `/api/admin/device-group` | 分组列表 |
| POST | `/api/admin/device-group/:id/device` | 添加设备到分组 |
| DELETE | `/api/admin/device-group/:id/device/:device_id` | 从分组移除设备 |
| GET | `/api/admin/device-group/:id/device` | 分组设备列表 |
| POST | `/api/admin/device/:id/tag` | 设置标签 |
| GET | `/api/admin/device/:id/tag` | 获取标签 |
| DELETE | `/api/admin/device/:id/tag/:key` | 删除标签 |
| GET | `/api/admin/device/tag/search` | 标签搜索设备 |

---

### 3.11 设备模拟器/在线调试

前端调试能力，集成在 `DeviceDetail.vue` 调试标签页。

**功能模块**：

| 模块 | 说明 |
|------|------|
| 模拟上报属性 | 基于物模型动态生成表单，支持 bool/int/float/enum，一键上报到 `/api/device/report` |
| 模拟上报事件 | 选择事件定义，填写输出参数，上报到 `/api/device/:id/event` |
| 调用服务 | 选择服务，动态渲染输入参数，调用 `/api/device/:id/service`，展示调用历史 |
| MQTT 原始报文 | 实时展示设备上下行 MQTT 报文，支持方向/主题筛选、自动刷新 |
| 在线调试日志 | 聚合操作日志，按类型筛选，分页展示 |

**MQTT 报文记录** (`MqttMessage`)：
- 自动记录上行（prop/post、event/post、service/reply）和下行（prop/set、service 调用）报文
- 存储 topic、direction(up/down)、payload、timestamp
- 支持 50 条最近记录查询

---

### 3.12 网关子设备管理

架构能力，支持网关/子设备拓扑关系管理。

**设备节点类型** (`Device.NodeType`)：
| 类型 | 说明 |
|------|------|
| `gateway` | 网关设备，可挂载子设备 |
| `device` | 直连设备，独立接入 |
| `sub_device` | 子设备，通过网关接入 |

**拓扑关系** (`DeviceTopology`)：
- 网关 ID + 子设备 ID + 状态 (online/offline)
- 支持批量添加/移除子设备
- 子设备在线状态同步更新

**API 接口**：

| 方法 | 路由 | 说明 |
|------|------|------|
| POST | `/api/admin/gateway/sub-device` | 添加子设备 |
| DELETE | `/api/admin/gateway/sub-device` | 移除子设备 |
| GET | `/api/admin/gateway/:gateway_id/sub-devices` | 网关下子设备列表 |
| GET | `/api/admin/device/:id/topology` | 设备拓扑信息 |
| PUT | `/api/admin/gateway/sub-device/status` | 更新子设备状态 |
| POST | `/api/admin/gateway/batch-sub-devices` | 批量添加子设备 |

---

### 3.13 配网功能

设备接入能力，支持主流配网方式。

**配网方式**：

| 方式 | 标识 | 适用场景 | 关键参数 |
|------|------|----------|----------|
| 蓝牙配网 | `ble` | BLE 设备近场配网 | PIN 码 (6位随机) |
| SoftAP 配网 | `softap` | Wi-Fi 设备热点配网 | SSID、BSSID |
| 扫码配网 | `qrcode` | 扫描设备二维码入网 | 二维码内容 (含 SN/PK) |
| 零配 | `zero` | 预置凭证设备 | 无需额外参数 |

**配网记录** (`DeviceProvisioning`)：
- 完整生命周期：pending → success/failed/timeout
- 记录方法、SSID、PIN码、二维码、错误信息
- 设备上线回调自动完成配网

**配网配置模板** (`ProvisioningConfig`)：
- 产品级配网参数模板
- BLE/SoftAP/扫码各自独立配置 JSON

**API 接口**：

| 方法 | 路由 | 说明 |
|------|------|------|
| POST | `/api/admin/provisioning/config` | 创建配网配置 |
| GET | `/api/admin/provisioning/config/:product_id` | 获取配置 |
| PUT | `/api/admin/provisioning/config/:product_id` | 更新配置 |
| POST | `/api/admin/provisioning/start` | 开始配网 |
| GET | `/api/admin/provisioning/status/:sn` | 配网状态 |
| POST | `/api/admin/provisioning/complete` | 完成配网(设备上线回调) |
| GET | `/api/admin/provisioning/records` | 配网记录列表 |
| GET | `/api/admin/provisioning/qrcode/:sn` | 生成设备配网二维码 |

---

### 3.14 数据流转/SQL分析

数据能力，支持灵活的数据路由与处理。

**数据流转规则** (`DataFlow`)：

| 字段 | 说明 |
|------|------|
| `type` | `republish` (重发布到 MQTT) / `forward` (HTTP 转发) / `sql` (SQL 处理) |
| `source_type` | `topic` (MQTT 主题) / `sql` (SQL 语句) |
| `source_topic` | 源 Topic，支持通配符 |
| `sql` | SQL 语句（如 `SELECT temp FROM /sys/+/+/prop/post WHERE temp > 30`） |
| `target_type` | `republish` / `mysql` / `timeseries` / `http` |
| `target_topic` | 目标 Topic (republish 时) |
| `target_config` | 目标配置 JSON (HTTP URL、MySQL 表名等) |

**执行记录** (`DataFlowExecution`)：记录每次流转的输入/输出、成功/失败、错误信息。

**API 接口**：

| 方法 | 路由 | 说明 |
|------|------|------|
| POST | `/api/admin/data-flow` | 创建流转规则 |
| GET | `/api/admin/data-flow` | 规则列表 |
| GET | `/api/admin/data-flow/:id` | 规则详情 |
| PUT | `/api/admin/data-flow/:id` | 更新规则 |
| DELETE | `/api/admin/data-flow/:id` | 删除规则 |
| PUT | `/api/admin/data-flow/:id/toggle` | 启用/禁用 |
| GET | `/api/admin/data-flow/:id/execution` | 执行记录 |

---

### 3.15 多协议网关

支持 CoAP/HTTP/Modbus 等非 MQTT 协议设备接入。

**协议网关** (`ProtocolGateway`)：
- 类型：`coap` / `http` / `mqtt` / `modbus`
- 监听地址/端口 + 协议特定配置 JSON
- 启用/禁用控制

**协议映射** (`ProtocolMapping`)：
- 网关 ID + 产品 ID
- 协议侧标识 → 物模型属性标识符
- 数据类型转换 + 缩放因子 + 偏移量

**API 接口**：

| 方法 | 路由 | 说明 |
|------|------|------|
| POST | `/api/admin/protocol-gateway` | 创建协议网关 |
| GET | `/api/admin/protocol-gateway` | 网关列表 |
| POST | `/api/admin/protocol-mapping` | 创建协议映射 |
| GET | `/api/admin/protocol-mapping` | 映射列表 |

---

### 3.16 设备诊断/监控增强

运维能力，主动发现设备异常。

**诊断记录** (`DeviceDiagnostic`)：

| 类型 | 说明 |
|------|------|
| `connectivity` | 连接性：离线、重连频繁、信号弱 |
| `performance` | 性能：延迟高、吞吐低、丢包 |
| `firmware` | 固件：版本过旧、升级失败 |
| `security` | 安全：异常登录、密钥泄露 |

| 级别 | 说明 |
|------|------|
| `info` | 信息：仅记录 |
| `warning` | 警告：需关注 |
| `critical` | 严重：需立即处理 |

| 状态 | 说明 |
|------|------|
| `open` | 待处理 |
| `resolved` | 已解决 |
| `ignored` | 已忽略 |

**指标统计** (`DeviceMetrics`)：按天聚合
- 在线时长 / 离线时长 (秒)
- 消息总数 / 错误数
- 平均延迟 (ms)

**API 接口**：

| 方法 | 路由 | 说明 |
|------|------|------|
| POST | `/api/admin/device-diagnostic` | 创建诊断记录 |
| GET | `/api/admin/device-diagnostic` | 诊断列表 |
| PUT | `/api/admin/device-diagnostic/:id/resolve` | 标记解决 |
| GET | `/api/admin/device/:sn/metrics` | 设备指标 (最近 N 天) |

---

## 四、响应式设计说明

全局断点：

| 断点 | 场景 | 布局策略 |
|------|------|---------|
| < 768px | 手机竖屏 | 侧边栏 → 汉堡菜单 + 抽屉；表格 → 卡片列表；图表 → 单列 |
| 768–1200px | 平板 / 窄桌面 | 侧边栏固定；两列栅格；图表并排 |
| ≥ 1200px | 桌面 / 大屏 | 多列布局；图表自由排列；表格显示全部字段 |

CSS 工具类定义在 [style.css](file:///d:/AI/eIOT/frontend/src/style.css)：

```
.mobile-only   — 仅手机显示
.tablet-only   — 仅平板显示
.pc-only       — 仅桌面显示
.hide-mobile   — 手机隐藏
```

### 典型响应式页面示例

**Layout.vue**：手机顶栏仅有 Logo + 汉堡菜单 + 用户下拉；桌面端左侧 220px 侧边栏常驻。

**Device.vue**：桌面端 `<el-table>` 显示 8 列；移动端切换为 `<el-card>` 卡片堆叠，每张卡片展示设备名、产品、在线状态、最新数据摘要。

**DeviceDetail.vue**：桌面端左侧"基本信息"、右侧"数据图表 + 控制面板"三栏；移动端所有区块堆叠为单列，图表自适应宽度。

---

## 五、RESTful API 汇总

### 5.1 认证接口

| 方法 | 路由 | 说明 | 鉴权 |
|------|------|------|------|
| POST | `/api/auth/login` | 手机号+密码登录 | 无 |
| POST | `/api/auth/send-code` | 发送短信验证码（6 位，10 分钟有效） | 无 |
| POST | `/api/auth/login-code` | 手机号+验证码登录 | 无 |
| GET | `/api/user/info` | 获取当前登录用户信息 | 用户 |

### 5.2 管理员接口（`/api/admin/*`，需 Admin 角色）

| 方法 | 路由 | 说明 |
|------|------|------|
| GET/POST | `/api/admin/project` | 项目列表 / 创建项目 |
| GET/PUT/DELETE | `/api/admin/project/:id` | 项目详情 / 更新 / 删除 |
| GET/POST | `/api/admin/product` | 产品列表 / 创建产品 |
| GET/PUT/DELETE | `/api/admin/product/:id` | 产品详情 / 更新 / 删除 |
| PUT | `/api/admin/product/:id/thing-model` | 更新产品物模型属性 |
| GET/PUT | `/api/admin/product/:id/mobile-ui` | 获取/保存移动端 UI 配置 |
| POST | `/api/admin/product/:id/thing-model/import` | 导入物模型 |
| GET | `/api/admin/product/:id/thing-model/export` | 导出物模型 |
| GET/POST | `/api/admin/device` | 设备列表 |
| POST | `/api/admin/device/batch` | 批量生成设备（含 bind_mode） |
| GET | `/api/admin/device/:id/bind-qr` | 获取设备三元组及绑定 URL |
| GET | `/api/admin/device/export/:product_id` | 导出某产品下设备 CSV（含三元组） |
| GET | `/api/admin/user` | 用户列表 |
| GET | `/api/admin/stats` | 看板统计数据 |
| POST/GET | `/api/admin/rule` | 规则引擎 CRUD |
| PUT | `/api/admin/rule/:id` | 更新规则 |
| DELETE | `/api/admin/rule/:id` | 删除规则 |
| PUT | `/api/admin/rule/:id/toggle` | 启用/禁用规则 |
| GET | `/api/admin/rule/:id/execution` | 规则执行记录 |
| POST/GET | `/api/admin/device-group` | 设备分组 CRUD |
| POST/DELETE | `/api/admin/device-group/:id/device` | 分组增删设备 |
| GET | `/api/admin/device-group/:id/device` | 分组设备列表 |
| POST/GET | `/api/admin/device/:id/tag` | 设备标签增删查 |
| DELETE | `/api/admin/device/:id/tag/:key` | 删除标签 |
| GET | `/api/admin/device/tag/search` | 标签搜索设备 |
| POST/DELETE | `/api/admin/gateway/sub-device` | 网关增删子设备 |
| GET | `/api/admin/gateway/:gateway_id/sub-devices` | 网关子设备列表 |
| GET | `/api/admin/device/:id/topology` | 设备拓扑信息 |
| PUT | `/api/admin/gateway/sub-device/status` | 更新子设备状态 |
| POST | `/api/admin/gateway/batch-sub-devices` | 批量添加子设备 |
| POST/GET | `/api/admin/provisioning/config` | 配网配置 CRUD |
| PUT | `/api/admin/provisioning/config/:product_id` | 更新配网配置 |
| POST | `/api/admin/provisioning/start` | 开始配网 |
| GET | `/api/admin/provisioning/status/:sn` | 配网状态 |
| POST | `/api/admin/provisioning/complete` | 完成配网(设备上线回调) |
| GET | `/api/admin/provisioning/records` | 配网记录列表 |
| GET | `/api/admin/provisioning/qrcode/:sn` | 生成设备配网二维码 |
| POST/GET | `/api/admin/data-flow` | 数据流转规则 CRUD |
| PUT/DELETE | `/api/admin/data-flow/:id` | 更新/删除流转规则 |
| PUT | `/api/admin/data-flow/:id/toggle` | 启用/禁用流转规则 |
| GET | `/api/admin/data-flow/:id/execution` | 流转执行记录 |
| POST/GET | `/api/admin/protocol-gateway` | 协议网关 CRUD |
| POST/GET | `/api/admin/protocol-mapping` | 协议映射 CRUD |
| POST/GET | `/api/admin/device-diagnostic` | 设备诊断 CRUD |
| PUT | `/api/admin/device-diagnostic/:id/resolve` | 标记诊断解决 |
| GET | `/api/admin/device/:sn/metrics` | 设备指标统计 |
| GET | `/api/admin/operation-log` | 操作日志 |
| GET | `/api/admin/alert-rule` | 告警规则列表 |
| DELETE | `/api/admin/alert-rule/:id` | 删除告警规则 |
| PUT | `/api/admin/alert-rule/:id/toggle` | 启用/禁用告警规则 |

### 5.3 用户接口（`/api/device/*`，普通用户 / 管理员均可）

| 方法 | 路由 | 说明 |
|------|------|------|
| POST | `/api/device/bind` | 绑定空闲设备（`device_sn`） |
| GET | `/api/device` | 当前用户的设备列表（含他人共享给我的） |
| GET | `/api/device/:id` | 设备详情（含最新数据、物模型、事件、服务、影子） |
| POST | `/api/device/:id/control` | 下发控制指令 |
| POST | `/api/device/:id/event` | 上报设备事件 |
| GET | `/api/device/:id/event` | 事件历史列表 |
| POST | `/api/device/:id/service` | 调用设备服务 |
| GET | `/api/device/:id/service` | 服务调用历史 |
| GET | `/api/device/:id/shadow` | 获取设备影子 |
| PUT | `/api/device/:id/shadow` | 设置设备期望值 |
| GET | `/api/device/data/:sn` | 获取属性历史数据 |
| POST | `/api/device/report` | 测试上报属性数据 |
| GET/PUT | `/api/device/:id/ui` | 用户个人 UI 配置（覆盖产品级） |
| POST | `/api/device/share` | 共享设备给其他用户 |
| DELETE | `/api/device/share/:id` | 撤销共享 |
| GET | `/api/device/share` | 共享列表 |
| GET | `/api/home` | 家庭列表 |
| POST | `/api/home` | 创建家庭 |
| GET | `/api/home/:id` | 家庭详情 |
| PUT | `/api/home/:id` | 更新家庭 |
| DELETE | `/api/home/:id` | 删除家庭 |
| POST | `/api/home/:id/member` | 添加家庭成员 |
| DELETE | `/api/home/:id/member/:uid` | 移除家庭成员 |
| GET | `/api/room` | 房间列表 |
| POST | `/api/room` | 创建房间 |
| PUT | `/api/room/:id` | 更新房间 |
| DELETE | `/api/room/:id` | 删除房间 |
| POST | `/api/room/device` | 房间添加设备 |
| DELETE | `/api/room/device` | 房间移除设备 |
| GET | `/api/scene` | 场景列表 |
| POST | `/api/scene` | 创建场景 |
| GET | `/api/scene/:id` | 场景详情 |
| PUT | `/api/scene/:id` | 更新场景 |
| DELETE | `/api/scene/:id` | 删除场景 |
| POST | `/api/scene/:id/run` | 执行场景 |
| GET | `/api/message` | 消息列表 |
| PUT | `/api/message/:id/read` | 标记消息已读 |
| POST | `/api/user/password` | 修改密码 |
| GET | `/api/user/operation-log` | 用户操作日志 |

---

## 六、Docker 部署

### 6.1 服务清单

| 容器 | 镜像 | 端口映射 | 说明 |
|------|------|---------|------|
| `eiot-mysql` | `mysql:8.0` | - | 业务数据库，命名卷持久化 |
| `eiot-redis` | `redis:7.2-alpine` | - | 缓存/验证码 |
| `eiot-emqx` | `emqx:5.7` | `18083:18083` | MQTT Broker + Web 控制台 |
| `eiot-backend` | 自定义（Alpine + `eiot-server`） | `8080:8080` | Go API 服务 |
| `eiot-frontend` | 自定义（Nginx + `dist/`） | `8088:80` | 管理台静态资源 |

### 6.2 部署步骤

```bash
# 1. 后端交叉编译
cd d:\AI\eIOT\backend
$env:GOOS="linux"; $env:GOARCH="amd64"
go build -o ../deploy/eiot-server ./cmd/api-server

# 2. 前端构建
cd d:\AI\eIOT\frontend
npm install
npm run build

# 3. 启动服务
cd d:\AI\eIOT\deploy
docker compose up -d --build

# 4. 验证
curl http://127.0.0.1:8080/api/admin/stats \
  -H "Authorization: Bearer <你的token>"
```

首次启动后端会自动执行 [seed.go](file:///d:/AI/eIOT/backend/internal/logic/seed.go)：
- 创建管理员（不存在时）
- 插入 5 个产品 + 50 台设备
- 5 个普通用户 + 20 条设备共享记录
- 每条表仅当为空时才写入，重复启动安全

### 6.3 访问入口

| 服务 | 地址 | 凭证 / 备注 |
|------|------|-------------|
| 管理台 Web | http://127.0.0.1:8088/ | `13800000000` / `admin123` |
| 后端 API | http://127.0.0.1:8080/ | 同上 |
| 内网 API（移动端用） | `http://192.168.31.29:8080` | 需确认本机内网 IP，防火墙放行 |
| EMQX 控制台 | http://127.0.0.1:18083/ | 初始账号 `admin` / `public` |

---

## 七、目录结构速查

```
d:\AI\eIOT\
├── 功能说明文档.md                    ← 本文件
├── 物联网综合管理平台需求文档.md       ← 原始需求文档
│
├── backend/                          # Go 后端
│   ├── go.mod / go.sum
│   ├── cmd/api-server/main.go        # 入口
│   ├── internal/
│   │   ├── dao/mysql.go              # MySQL
│   │   ├── handler/handlers.go       # Gin 路由 + 业务处理
│   │   ├── logic/
│   │   │   ├── business.go           # 业务逻辑
│   │   │   └── seed.go               # 种子数据
│   │   ├── model/models.go           # 数据模型（User/Product/Device/Share/Log/Rule/Group/Tag/Topology/Provisioning/DataFlow/ProtocolGateway/Diagnostic/Metrics/MqttMessage）
│   │   └── svc/servicecontext.go     # 服务上下文
│   └── pkg/
│       ├── config/config.go          # YAML 配置解析
│       ├── middleware/auth.go        # JWT + 角色中间件
│       ├── middleware/ratelimit.go   # 速率限制
│       ├── cache/redis.go            # Redis 封装
│       ├── mqtt/emqx.go              # EMQX 消息发布
│       ├── util/crypto.go            # bcrypt + AES-CBC
│       └── tsdb/tdengine.go          # 时序数据（预留）
│
├── frontend/                         # Vue3 + Vite 前端
│   ├── package.json / vite.config.js
│   ├── index.html
│   ├── src/
│   │   ├── main.js / App.vue / style.css
│   │   ├── router/index.js           # 路由配置
│   │   ├── utils/request.js          # Axios 封装
│   │   └── views/
│   │       ├── Login.vue             # 登录
│   │       ├── Layout.vue            # 响应式布局
│   │       ├── Dashboard.vue         # 看板
│   │       ├── Project.vue           # 项目管理（飞燕层级）
│   │       ├── Product.vue           # 产品 + 物模型管理 (事件/服务编辑器 + 导入导出)
│   │       ├── Device.vue            # 设备列表
│   │       ├── DeviceDetail.vue      # 设备详情 + 图表 + 控制 + 调试(事件/服务/影子/历史图表/MQTT/日志)
│   │       ├── MobileUI.vue          # 拖拽式移动端 UI 配置
│   │       ├── Share.vue             # 设备共享
│   │       ├── Users.vue             # 用户管理
│   │       └── ThingModel.vue        # 原物模型页（保留兼容）
│   └── dist/                         # 构建产物（Nginx 直接 serve）
│
├── mobile/                           # Flutter 移动端
│   ├── lib/
│   │   ├── api/api_client.dart       # HTTP 客户端
│   │   ├── models/models.dart        # 数据模型 (ThingProperty增强/ThingEvent/ThingService/DeviceDetail)
│   │   ├── pages/                    # 页面
│   │   │   ├── login_page.dart
│   │   │   ├── device_list_page.dart
│   │   │   ├── device_detail_page.dart  # 5标签页: 属性/事件/服务/历史/影子
│   │   │   ├── share_page.dart
│   │   │   ├── profile_page.dart
│   │   │   └── home_page.dart
│   │   ├── providers/                # 状态管理
│   │   └── main.dart
│   └── android/                      # Android 原生工程
│
├── deploy/                           # Docker 部署
│   ├── docker-compose.yml            # 一键拉起
│   ├── Dockerfile.backend            # 后端镜像
│   ├── Dockerfile.frontend           # 前端镜像
│   ├── nginx.conf                    # 前端 Nginx 配置
│   ├── config.yaml                   # 后端运行时配置
│   └── eiot-server                   # 交叉编译的后端二进制
│
└── 功能说明文档.md
```

---

## 八、已验证功能清单（自测通过）

| 功能 | 验证方式 | 结果 |
|------|---------|------|
| 管理员登录 JWT 签发 | curl `/api/auth/login` + 解码 payload | ✅ |
| 产品列表返回 6 个产品 | `GET /api/admin/product` | ✅ |
| 产品详情含完整 properties/events/services | `GET /api/admin/product/1` | ✅ |
| 产品物模型导入/导出 | `POST/GET /api/admin/product/1/thing-model/import\|export` | ✅ |
| 移动端 UI 配置可写入/读取 | `GET/PUT /api/admin/product/1/mobile-ui` | ✅ |
| 设备列表返回 60 台 | `GET /api/admin/device` | ✅ |
| 普通用户仅看到自己的 + 共享的设备 | `GET /api/device`（用户视角） | ✅ |
| 设备详情含最新数据 + 物模型 + 事件/服务/影子 | `GET /api/device/1` | ✅ |
| 控制指令下发成功 | `POST /api/device/1/control` | ✅ |
| 设备事件上报/查询 | `POST/GET /api/device/1/event` | ✅ |
| 设备服务调用/历史 | `POST/GET /api/device/1/service` | ✅ |
| 设备影子读写 | `GET/PUT /api/device/1/shadow` | ✅ |
| 设备属性历史数据/ECharts图表 | `GET /api/device/data/:sn` | ✅ |
| 规则引擎 CRUD + 执行记录 | `POST/GET/PUT/DELETE /api/admin/rule` | ✅ |
| 设备分组/标签/搜索 | `POST/GET /api/admin/device-group\|tag` | ✅ |
| 网关子设备管理 | `POST/GET/DELETE /api/admin/gateway/*` | ✅ |
| 配网功能 (BLE/SoftAP/扫码) | `POST/GET /api/admin/provisioning/*` | ✅ |
| 数据流转规则 | `POST/GET /api/admin/data-flow` | ✅ |
| 多协议网关 | `POST/GET /api/admin/protocol-gateway` | ✅ |
| 设备诊断/指标统计 | `POST/GET /api/admin/device-diagnostic` | ✅ |
| 看板统计返回多项指标 | `GET /api/admin/stats` | ✅ |
| Web 管理台可访问并正常渲染 | 浏览器打开 `http://127.0.0.1:8088` | ✅ |
| 前端构建成功 | `npm run build` | ✅ |
| 移动端构建成功 | `flutter build apk` | ✅ |
| LDPlayer 模拟器运行 App 正常 | `adb install` + 启动验证 | ✅ |
| 响应式布局：切换手机/桌面尺寸 UI 正确切换 | 浏览器 DevTools 切换设备模式 | ✅ |
| Docker Compose 一键部署 | `docker compose up -d --build` | ✅ |
| 综合 API 测试 (29/30 通过) | `test_thingmodel.ps1` | ✅ |

---

## 九、关键设计决策

| 决策 | 内容 | 理由 |
|------|------|------|
| 产品即物模型 | 物模型字段内联到 Product 表，不再独立建 thing_model 表 | 简化结构，避免 N+1 关联，逻辑更直观 |
| JWT 会话无状态 | 服务端不存储 session，仅校验签名 | 横向扩展方便；Redis 仅做黑名单/验证码 |
| 多租户数据隔离 | 所有设备查询强制加 `owner_id = ? OR device_shares.to_user_id = ?` 条件 | 用户只可见自己的 + 共享的设备 |
| 响应式单一代码库 | Web 管理台同一套代码在 CSS 断点切换布局 | 节省维护成本；移动 Web 页面无需独立项目 |
| 拖拽式 UI 生成 | 管理员在 Web 端拖拽配置，产物为 JSON，App 加载后按模板渲染 | 无需每次新增设备都改 App 代码 |
| 种子数据幂等 | `seed.go` 每个表先 `COUNT`，为空才写入 | 重复启动不报错、不重复插入 |
| Alpine 容器 + 静态编译 | 后端二进制单文件，Alpine 做基础镜像 | 镜像 < 20MB，秒级启动，安全补丁少 |

---

## 十、下一步规划（待实现 / 待优化）

1. **Android App 完整开发**：当前目录脚手架已创建，需补充登录页、设备列表页、详情页、共享页并打包 APK。
2. **TDengine 时序数据落库**：目前 `latest_json` 仅存快照，需在 EMQX 订阅回调中将完整上报写入 TDengine 并支持历史曲线查询。
3. **设备在线状态实时刷新**：目前设备 `status` 依赖上报时更新，可改用 EMQX 提供的 `$SYS` 主题监听设备上下线。
4. **短信验证码真实服务**：当前验证码仅落 Redis，未对接真实短信网关。
5. **设备固件 OTA 升级**：扩展产品表增加 `firmware_version`、OTA 任务管理（框架已就绪，需完善）。
6. **告警与通知**：基于物模型 events 定义告警规则，对接钉钉/邮件/推送通知。
7. **App 国际化**：多语言资源包。
8. **HTTPS + 域名**：生产环境启用 Let's Encrypt，替换 8080/8088 为标准 443。
9. **TDengine 时序数据落库**：目前 `latest_json` 仅存快照，需在 EMQX 订阅回调中将完整上报写入 TDengine 并支持历史曲线查询。
10. **规则引擎定时触发器**：补充 cron 表达式解析与定时任务调度器。

---

**文档维护者**：EIOT 开发团队
**最后更新**：2026-08-30
**适用版本**：平台 v2.0

# 物联网综合管理平台需求文档（Gin + EMQX + Redis + MySQL + TDengine + Vue3 + MAUI）最终定稿
## 一、项目概述
本项目是一款**严格多租户隔离、产品即物模型、100%兼容阿里云飞燕物模型协议、管理员批量设备管理、用户扫码绑定设备、跨用户设备共享**的轻量化高性能物联网平台。

平台采用云原生高性能技术栈，基于 **Gin 高性能Web框架** 开发后端，EMQX 承担设备消息通信，TDengine 承载海量时序数据；Vue3 管理后台支持 Web 看板拖拽、移动端 UI 拖拽生成；MAUI 实现跨平台移动端 APP。整体需求逻辑闭环、标准化程度高，可直接用于 AI 全流程自动编码、项目落地与迭代开发。

### 1.1 固定技术栈（不可修改）
- **后端框架**：Gin（Golang 高性能 Web 框架，RESTful API、中间件生态完善，适配高并发IoT场景）
- **物联网消息服务**：EMQX（高性能 MQTT 服务器）
- **缓存服务**：Redis（设备状态、在线缓存、Token、验证码）
- **业务数据库**：MySQL（结构化业务数据）
- **时序数据库**：TDengine（唯一指定时序数据存储）
- **Web管理后台**：Vue3 + Vite + Element Plus + 拖拽可视化引擎 + **响应式布局**
- **移动端APP**：MAUI 跨平台（Android/iOS/Windows）

### 1.2 核心能力摘要
- 核心规则：**一个产品 = 一个物模型 + 一套产品级移动端UI模板**，三者强绑定，产品本身即为物模型载体，不再独立拆分物模型模块，结构极简统一
- 产品内置物模型完全兼容阿里云飞燕平台 JSON 格式，支持双向导入、导出
- 管理员专属权限：产品（物模型）管理、产品级移动端UI配置、批量设备生成与运维
- 普通用户权限：仅可注册登录、扫码绑定空闲设备、使用设备、管理自有设备共享
- 严格多租户数据隔离，用户资源相互隔离，仅可通过设备共享跨用户授权
- EMQX 实现设备双向消息通信，Redis 秒级维护设备在线状态，TDengine 持久化全量时序数据
- Vue3 后台支持**Web看板拖拽布局 + 可视化拖拽生成产品级MAUI移动端控制UI**，整体页面**全尺寸响应式适配**
- MAUI 移动端根据设备所属产品，自动加载对应产品物模型与绑定的移动端UI界面，支持设备控制、数据查看、共享管理

## 二、整体业务架构
### 2.1 核心业务层级（固定逻辑）
管理员 → 产品（内置物模型） ↔ 产品级移动端UI模板 → 批量设备生成 → 用户绑定/共享 → 设备数据上报 → 时序数据存储 & 状态缓存
1. 绑定规则：**单个产品自身即为一套独立物模型，同时绑定唯一产品级移动端UI模板**，三者一一对应，不可拆分、不可多对多复用
2. 一个产品下可包含无数台设备，同产品下所有设备共用该产品的物模型结构与移动端UI模板
3. 所有设备默认私有归属，用户数据严格隔离
4. 仅设备所有者主动共享授权，可突破用户数据隔离限制

### 2.2 整体数据流
- 设备通过 MQTT 协议连接 EMQX 服务，上报属性、事件、心跳、状态数据
- **Gin 后端** 订阅 EMQX 主题，完成数据解析、校验、业务逻辑处理、接口响应
- 设备在线状态、最新点位数据缓存至 Redis，实现秒级状态更新
- 设备全量历史时序数据统一存入 TDengine，用于曲线展示、历史回溯、数据统计
- 用户、产品（物模型配置）、设备、产品移动端UI配置、共享记录等业务数据存入 MySQL
- Vue3 后台、MAUI 移动端调用 Gin 接口拉取业务数据与时序数据，实现数据展示、设备控制、UI 渲染

## 三、用户体系与权限（最终闭环）
### 3.1 注册与登录
- 支持**手机号验证码注册、登录**
- 支持**邮箱验证码注册、登录**
- 支持账号密码登录、记住登录状态、密码找回功能

### 3.2 角色权限严格区分
#### 3.2.1 管理员（超级权限）
- 拥有系统全部操作权限
- 可新建、编辑、删除产品（物模型），支持产品物模型配置、导入导出、产品级移动端UI配置
- 可在产品下批量生成设备、导出设备台账
- 可管理全量用户、查看全量设备、系统日志、操作记录
- 可使用拖拽编辑器，配置、修改、重置**产品专属移动端控制UI模板**

#### 3.2.2 普通注册用户（受限权限）
- **无任何产品（物模型）、产品级移动端UI的创建、编辑、删除权限**
- 仅可扫码绑定平台空闲、未归属任何用户的设备
- 仅可查看、操作自己绑定的设备、他人共享给自己的设备
- 可对自有设备发起共享、调整共享权限、取消共享
- 可基于产品默认UI模板，自定义单设备个人移动端UI、自定义个人 Web 看板拖拽布局
- 无法越权查看、操作其他用户私有资源

### 3.3 多租户隔离规则（铁规）
- 所有业务数据表强制携带归属用户ID，查询、操作强制隔离
- 用户之间默认数据完全隔离，无任何越权查询、操作入口
- 唯一跨用户数据访问方式：设备所有者主动授权共享

## 四、产品（物模型合一）管理模块（仅管理员）
### 4.1 兼容标准
产品内置物模型**100%双向兼容阿里云飞燕IoT平台物模型JSON格式**，字段结构、数据类型、读写权限、事件参数、单位定义完全对齐，支持无差别导入、导出。

### 4.2 整体说明
产品集成物模型能力，直接在产品内定义设备可上报、可控制的点位信息，分为**设备属性、设备事件、设备功能**三大类，所有点位以列表形式展示、编辑、管理。

### 4.3 物模型通用字段（全类型点位统一字段）
| 字段名称 | 字段说明 | 约束要求 |
| ---- | ---- | ---- |
| 点位标识ID | 唯一编码，全局不可重复，用于后端解析、MQTT报文匹配、TDengine存储映射 | 必填、不可修改 |
| 点位名称 | 中文展示名称，用于前端/移动端页面展示 | 必填 |
| 物理量 | 描述该点位对应的物理含义（如温度、湿度、电压、开关状态、运行模式等） | 必填 |
| 数据类型 | 限定数据存储与传输类型，枚举值：整型(int)、浮点型(float)、布尔型(bool)、字符串型(string)、枚举型(enum) | 必填 |
| 读写权限 | 枚举值：只读(设备上报，平台仅查看)、可读写(平台可下发指令控制设备) | 必填 |
| 单位 | 物理量计量单位（如℃、%、V、A、kW、m/s等，无物理量则填空） | 选填 |
| 取值范围 | 数据合法区间，格式：最小值~最大值；布尔/字符串/枚举类型可填空 | 选填 |
| 数据精度 | 浮点型专属，保留小数位数（如0=整数、1=保留1位小数） | 选填 |
| 枚举选项 | 枚举类型专属，键值对形式定义可选内容（如0=关机、1=运行、2=故障） | 数据类型为enum时必填 |
| 备注说明 | 点位补充描述、使用场景说明 | 选填 |

### 4.4 分类列表管理（可视化编辑/查看列表）
#### 4.4.1 设备属性列表（核心数据点位，周期上报）
用于采集设备运行状态、环境数据、参数数值，为时序数据主要存储对象。
1. 列表展示列：点位标识ID、点位名称、物理量、数据类型、读写权限、单位、取值范围、最新数据、操作
2. 支持操作：新增、编辑、删除、批量导入、批量导出、筛选、搜索、排序
3. 配置示例
    - 点位ID：temp_01，点位名称：环境温度，物理量：环境温度，数据类型：float，读写权限：只读，单位：℃，取值范围：-40~85，数据精度：1
    - 点位ID：light_switch，点位名称：灯光开关，物理量：通断状态，数据类型：bool，读写权限：可读写，单位：无，取值范围：无

#### 4.4.2 设备事件列表（被动触发类消息）
设备异常、告警、动作触发时主动上报的事件信息，无持续数值，仅记录触发状态与时间。
1. 列表展示列：点位标识ID、事件名称、物理量、事件类型、触发说明、上报参数、操作
2. 事件类型枚举：告警事件、故障事件、操作事件、离线事件
3. 支持操作：新增、编辑、删除、筛选、搜索

#### 4.4.3 设备功能列表（平台主动下发控制指令）
设备可被远程调用的功能接口，对应平台/移动端控制动作。
1. 列表展示列：点位标识ID、功能名称、物理量、数据类型、控制参数、权限、操作
2. 支持操作：新增、编辑、删除、筛选、搜索

### 4.5 物模型导入/导出功能
1. **导入**
    - 支持上传阿里云飞燕标准JSON文件，系统自动解析并批量生成当前产品下三类点位列表
    - 导入校验：重复点位ID拦截、数据格式校验、字段补全提示
2. **导出**
    - 将当前产品内全量点位数据，转换为阿里云飞燕标准JSON格式文件
    - 导出内容包含：属性列表、事件列表、功能列表及所有配置字段

### 4.6 产品（物模型）锁定规则
1. 产品创建完成后，若**已在该产品下创建设备**，产品内所有点位结构永久锁定，禁止新增、编辑、删除点位，保障存量设备数据结构统一。
2. 若需修改点位、新增物理量、调整数据类型/权限，必须新建产品并重新配置点位。
3. 无任何下属设备的空产品，支持点位全量编辑、导入导出、删除操作。

## 五、产品与设备管理
### 5.1 产品管理（仅管理员）
1. 核心定义：**产品 = 物模型 + 产品级移动端UI模板**，三者原生绑定，创建产品时同步完成基础配置。
2. 管理员可创建产品、配置产品点位（物模型）、编辑产品基础信息、配置产品专属移动端UI模板。
3. 仅无任何下属设备的空产品，允许删除操作。

### 5.2 批量创建设备（仅管理员）
管理员在指定产品下批量生成设备，快速量产设备台账：
- 自定义设备名称前缀、批量生成数量
- 系统自动生成唯一设备SN、设备密钥、设备名称
- 支持批量导出设备Excel台账，用于生产、部署、对账
- 同产品下所有设备，默认继承当前产品的点位结构（物模型）与产品级移动端UI模板

## 六、设备核心业务逻辑
### 6.1 设备基础能力
- 设备通过 SN + Secret 双重认证接入 EMQX，保障接入安全
- Redis 实时维护设备在线/离线状态、最后在线时间、最新上报数据
- 支持设备属性、事件、状态实时展示
- TDengine 存储全量历史时序数据，支持曲线绘制、历史数据回溯与统计
- 根据产品点位读写权限，平台与移动端通过 Gin 接口下发远程指令，实现设备控制
- 设备所属产品决定其物模型结构与默认移动端UI，同产品设备共用一套标准

### 6.2 移动端扫码绑定规则
- 仅**未归属任何用户的空闲设备**允许扫码绑定
- 扫码绑定成功后，设备永久归属当前登录用户
- 已归属用户的设备，禁止二次扫码抢占绑定
- 已有归属的设备，仅可通过所有者共享授权获取使用权限

### 6.3 设备共享规则（最终闭环）
- 仅设备真实归属所有者可发起共享、修改共享权限、取消共享
- 共享权限分为：只读权限、可控制权限
- 支持永久共享、限时共享（自定义有效期）
- 被共享用户可在个人账号下查看、使用、操作授权范围内的设备
- 被共享用户**无设备归属权、不可删除设备、不可转移设备、不可二次共享**
- 所有者可随时取消共享，权限实时回收、立即生效

## 七、数据存储规范（固定最终版）
### 7.1 MySQL 存储（结构化业务数据）
存储用户、角色、产品（含内置物模型全量点位配置）、设备、产品级移动端UI配置、用户个人设备UI配置、共享记录、操作日志、权限记录、用户Web看板拖拽布局配置等所有业务结构化数据。

### 7.2 Redis 存储（高速缓存数据）
存储设备在线状态、设备最新点位数据、用户登录Token、短信/邮箱验证码、EMQX临时连接状态，支撑高并发、低延迟访问。

### 7.3 TDengine 存储（时序数据）
采用 TDengine 物联网标准架构：**超级表统一产品物模型模板、单设备独立子表**，存储所有设备的属性上报记录、时间戳、数值、状态数据，用于数据曲线、历史查询、数据统计分析。

### 7.4 EMQX 消息体系
上行消息：设备属性上报、事件上报、心跳保活、状态上报
下行消息：平台控制指令、参数配置、设备操作指令

Gin 服务作为 EMQX 客户端，订阅/发布对应主题完成消息流转。

## 八、Vue3 管理后台核心功能（含响应式+UI绑定规则）
### 8.1 基础运维功能
- 管理员：产品（物模型）管理、产品移动端UI配置、批量设备生成、设备运维、用户管理、系统日志、全量数据查看
- 普通用户：个人中心、自有设备管理、共享设备管理、设备数据查看、设备权限管理、个人设备UI自定义

### 8.2 前端全局响应式布局要求
1. **适配终端**
   - 桌面端：大屏显示器、常规显示器、窄屏笔记本
   - 平板设备：横屏 / 竖屏自适应
   - 手机浏览器：完整兼容移动端H5访问，页面自动重排、缩放、折叠
2. **断点规则（统一采用）**
   - 大屏：≥1920px，多列平铺、完整展示所有菜单与面板
   - 桌面：1200px ~ 1919px，标准布局，侧边栏正常展开
   - 平板：768px ~ 1199px，侧边栏自动折叠为图标模式，表格、卡片自动缩列
   - 手机：＜768px，顶部导航替代侧边栏，多列布局改为单列流式布局，表格横向滚动
3. **组件响应式规则**
   - 数据卡片：大屏4列 → 桌面3列 → 平板2列 → 手机1列
   - 数据表格：小屏自动隐藏非核心列，保留关键字段，支持横向滑动查看全列
   - 表单、弹窗：宽度自适应，小屏全屏展示，控件竖向排列
   - ECharts 图表：自动监听窗口变化，等比例缩放、重绘
4. **拖拽编辑器响应式**
   - Web看板编辑器、移动端UI拖拽编辑器：画布区域自适应容器尺寸
   - 组件库、属性面板在小屏可收起/展开，保证编辑区域可用

### 8.3 拖拽式移动端UI可视化生成（核心特色功能）
1. **绑定规则**
   - 每个产品拥有**唯一专属移动端UI模板**，由管理员统一拖拽配置，作为该产品所有设备的默认UI。
   - UI编辑器仅加载当前产品内置的点位（物模型），禁止跨产品调用点位。
2. **点位自动匹配组件**：自动加载当前产品所有点位，数值点位匹配数据卡片、布尔点位匹配开关、档位点位匹配滑动条/下拉框、事件点位匹配功能按钮。
3. **自由可视化布局**：支持组件拖拽移位、缩放、增删、调整层级、边距间距，实时预览手机端真实展示效果。
4. **专用移动端组件库**：内置数据卡片、时序曲线、开关控制、数值调节、功能按钮、状态指示灯、告警提示等物联网通用组件。
5. **权限自动联动**：只读点位仅展示数据、禁止操作；可写点位开放控制能力，严格遵循产品点位权限定义。
6. **分层配置策略**
   - 第一层：**产品级默认UI模板**（管理员配置，全产品设备共用）
   - 第二层：**用户个人设备UI**（普通用户基于产品默认模板自定义，仅当前用户单设备生效）
   - 优先级：用户个人UI > 产品默认UI
7. **模板保存与切换**：产品级UI支持多套模板保存、一键切换、一键重置；用户个人UI独立保存，多租户配置完全隔离。
8. **实时同步MAUI端**：后台配置保存后，移动端通过 Gin 接口自动拉取对应产品/设备的最新UI结构与组件配置，实时刷新控制界面。
9. **系统模板兜底**：所有产品默认自带标准移动端UI模板，零配置即可正常使用。
10. **锁定规则**：产品下已创建设备后，产品级移动端UI模板仅支持编辑，不允许整体重置清空，保证存量设备正常展示。

### 8.4 拖拽式Web端自定义数据看板
全用户通用，支持管理员、普通用户自定义后台数据看板布局，纯拖拽无代码配置。
- 内置物联网可视化组件：设备统计卡片、实时仪表盘、时序曲线、设备状态列表、告警组件、分组卡片等。
- 支持组件拖拽、缩放、增删、分层排版，自适应页面尺寸。
- 组件可一键绑定产品点位，自动渲染对应数据与图表。
- 支持多套看板布局保存、切换、重置，用户配置相互隔离。
- 系统预置监控、统计、状态总览通用模板，支持二次修改。

## 九、MAUI 移动端 APP 功能
- 手机号、邮箱账号登录与注册
- 扫码绑定平台空闲设备
- 自有设备、共享设备分类列表展示
- 自动识别设备所属产品，优先加载**用户个人自定义UI**，无自定义则加载**产品默认移动端UI模板**
- 设备实时数据查看、历史时序曲线查询
- 基于产品点位权限的设备远程控制、参数调节
- 设备共享授权、权限修改、取消共享
- 个人账号信息管理、密码修改

## 十、兼容性规范
本系统产品内置物模型**100%双向兼容阿里云飞燕生活物联网平台**，导入导出无字段丢失、无格式错乱、无结构偏差，可无缝双向迁移。

## 十一、非功能需求
- 高并发：基于 **Gin + EMQX** 支撑大规模设备并发在线与数据上报
- 低延迟：设备状态、数据更新秒级响应
- 强安全：设备密钥认证、用户权限隔离、越权拦截、数据隔离
- 高可扩展：新增设备类型直接通过新建产品、配置产品点位实现，无需修改代码
- **前端兼容性**：Vue3 响应式页面兼容主流现代浏览器（Chrome、Edge、Firefox）及各版本手机自带浏览器

## 十二、项目整体模块结构
### 后端（Gin 框架）
用户权限模块、产品（物模型合一）模块（点位列表CRUD、导入导出、字段校验）、设备模块、EMQX 消息订阅/发布服务、Redis 缓存服务、TDengine 时序数据服务、设备共享模块、日志模块、拖拽UI配置模块（产品级UI、用户个人UI）、全局中间件（鉴权、跨域、限流、日志、参数校验）

### Vue3 管理后台
管理员运维模块、用户设备模块、Web拖拽看板模块、**产品专属移动端拖拽UI生成模块**、数据可视化模块、共享管理模块、系统设置模块、**全局响应式布局适配模块**

### MAUI 移动端
账号体系模块、扫码绑定模块、设备展示与控制模块、**动态UI渲染模块（按产品加载对应UI）**、共享管理模块、个人中心模块

## 十三、统一开发、部署、测试环境（强制唯一）
全项目开发、运行、测试环境**统一固化，无任何备选方案**，保证团队环境一致、AI编译部署无差异。

### 13.1 开发操作系统
Windows 10 / Windows 11

### 13.2 服务部署规范
MySQL、Redis、EMQX、TDengine、**Gin 后端**、Vue3 前端，**全部基于 Rancher Desktop（WSL2）容器化运行**，禁止本地裸机安装任何中间件与服务。

### 13.3 移动端测试环境
MAUI APP 编译、安装、运行、功能测试统一使用**雷电安卓模拟器**。

### 13.4 全自动编译、运行、测试规范
- Gin 后端：一键编译、一键容器部署、自动启动、自动初始化数据库结构与基础数据
- Vue3 前端：一键打包、自动部署、热更新运行，**响应式布局同步打包生效**
- 所有容器服务支持一键启动、重启、重置
- MAUI 项目：一键编译安卓APK、自动安装至雷电模拟器、自动启动应用
- 全链路自动化测试：自动校验 EMQX 设备连接、数据上报、TDengine 时序存储、用户权限隔离、扫码绑定、设备共享、拖拽UI渲染、**产品即物模型绑定关系**、多终端响应式展示等全核心功能

### 13.5 网络互通要求
Windows 主机、Rancher Desktop 容器集群、雷电模拟器三者网络完全互通，保障前后端联调、设备模拟接入、全功能测试闭环可用。